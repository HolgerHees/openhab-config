variables:
  title: ""
  item: ""
  icon: "oh:selfsettings"
  iconstate: ""
  display: ""

component: li
config:
  style:
    <<: !if
      if: display
      then:
        display: "=${display} ? '' : 'none'"
  class:
    - input
slots:
  default:
    - component: oh-icon
      config:
        class:
          - icon
        height: 32px
        icon: "${icon}"
        <<: !if
            - if: iconstate
              then:
                state: "=${iconstate}"
            - elseif: item
              then:
                state: "=items['${item}'].state"
    - component: Label
      config:
        class:
          - name
        text: "${title}"
    - component: oh-input
      config:
        item: ${item}
